# Аудит сервисов Activities

Этот файл содержит результаты последовательного аудита каждого сервиса в папке `services/activities/` на соответствие `tools_schema.md` и возможностям SDK.

---

## calls.go
**Layer:** calls
**Schema actions:** create (write-only)
**SDK service:** CallsService (`core/services/calls.go`)

| Метод SDK | Реализован в сервисе | Метод сервиса | Комментарий |
|-----------|----------------------|----------------|-------------|
| CreateOne | ✅ | `CreateCall` | Соответствует схеме и SDK |
| Get | ❌ | - | Заблокировано в SDK (ErrNotAvailableForAction) |
| GetOne | ❌ | - | Заблокировано в SDK (ErrNotAvailableForAction) |
| Update | ❌ | - | Заблокировано в SDK (ErrNotAvailableForAction) |

**Статус:** ✅ Полностью соответствует (с учетом ограничений API)
**TODO:** Нет

### Capabilities Coverage

**Filters:**
- ℹ️ Звонки в amoCRM API v4 — это write-only сущность. Фильтров для `GET /calls` не существует. Чтение звонков происходит через слой `events` или `notes`.

**Batch Operations:**
- ❌ SDK: `BaseEntityService.Create` поддерживает массив звонков.
- ❌ Bot: Только по одному через `CallData`.

**With/Relations:**
- ℹ️ Не поддерживается API для звонков.

**Рекомендации:**
1. Оставить как есть, так как массовое создание звонков — редкий кейс для AI.

---

## events.go
**Layer:** events
**Schema actions:** list, get (read-only)
**SDK service:** EventsService (`core/services/events.go`)

| Метод SDK | Реализован в сервисе | Метод сервиса | Комментарий |
|-----------|----------------------|----------------|-------------|
| Get | ✅ | `ListEvents` | С фильтрацией по сущности |
| GetOne | ✅ | `GetEvent` | |
| Add | ❌ | - | Заблокировано в SDK (read-only) |
| Update | ❌ | - | Заблокировано в SDK (read-only) |

**Статус:** ⚠️ Частично (фильтры отсутствуют)
**TODO:** Нет

### Capabilities Coverage

**Filters:**
| SDK Filter Method | Bot Field | Status | Comment |
|-------------------|-----------|--------|---------|
| SetLimit | - | ❌ | Hardcoded 50. |
| SetPage | - | ❌ | Hardcoded 1. |
| SetIDs | - | ❌ | |
| SetTypes | - | ❌ | Нельзя увидеть только "создание сделок" или "изменение статусов". |
| SetEntity | - | ✅/❌ | Жестко привязано к `input.parent.type`. Нельзя получить события по всему аккаунту. |
| SetEntityIDs | - | ✅/❌ | Жестко привязано к `input.parent.id`. |
| SetCreatedAt | - | ❌ | |
| SetCreatedBy | - | ❌ | |

**Batch Operations:**
- ℹ️ Events — read-only слой, батч-создания нет по определению.

**With/Relations:**
- ❌ Не используются.

**Рекомендации:**
1. Позволить вызов `list` без `parent` (события по всему аккаунту).
2. Добавить `models.EventsFilter` для фильтрации по типам событий (`types`) и времени.


---

## files.go
**Layer:** files
**Schema actions:** list, link, unlink
**SDK service:** EntityFilesService (`core/services/entity_files.go`)

| Метод SDK | Реализован в сервисе | Метод сервиса | Комментарий |
|-----------|----------------------|----------------|-------------|
| Get | ✅ | `ListFiles` | Лимит 50 |
| Link | ✅ | `LinkFiles` | |
| Unlink | ✅ | `UnlinkFile` | |

**Genkit Tool Handler:**
- 🛠 `handleFiles` корректно обрабатывает все три экшена (`list`, `link`, `unlink`). Проверки на наличие `FileUUIDs`/`FileUUID` присутствуют.

**Статус:** ⚠️ Частично (фильтры и глобальный Drive отсутствуют)
**TODO:** Нет

### Capabilities Coverage

**Filters:**
| SDK Filter Method | Bot Field | Status | Comment |
|-------------------|-----------|--------|---------|
| SetLimit | - | ❌ | Hardcoded 50. |
| SetPage | - | ❌ | Hardcoded 1. |
| SetUUID | - | ❌ | |
| SetName | - | ❌ | |
| SetExtensions | - | ❌ | Нельзя отфильтровать только "договоры в PDF". |
| SetTerm | - | ❌ | Нет поиска по названию файла. |
| SetDatePreset | - | ❌ | |

**Drive Support (Global):**
- ❌ SDK: `FilesService` позволяет искать файлы по всем дискам, удалять и обновлять их.
- ❌ Bot: Работает только в контексте сущности (`EntityFilesService`). AI не может ответить на вопрос "найди последние документы в CRM", не зная к какой сделке они привязаны.

**Upload:**
- ❌ SDK: Поддерживает `UploadOne` (загрузка файлов в облако).
- ❌ Bot: Не реализовано.

**Batch Operations:**
- ✅ SDK: `Link` принимает массив `file_uuids`.
- ✅ Bot: `handleFiles` и `LinkFiles` работают со слайсом `FileUUIDs`. Массовое прикрепление работает.

**With/Relations:**
- ❌ Не используются.

**Рекомендации:**
1. Вынести `FilesFilter` в модель, чтобы AI мог искать файлы по расширению или названию.
2. Добавить глобальный экшен `search` (без `parent`), использующий `FilesService` для поиска по всему диску.


---

## links.go
**Layer:** links
**Schema actions:** list, link, unlink
**SDK service:** LinksService (`core/services/links.go`)

| Метод SDK | Реализован в сервисе | Метод сервиса | Комментарий |
|-----------|----------------------|----------------|-------------|
| Get | ✅ | `ListLinks` | |
| Link | ✅ | `LinkEntity` | |
| Unlink | ✅ | `UnlinkEntity` | |

**Genkit Tool Handler:**
- 🛠 `handleLinks` корректно обрабатывает `list`, `link`, `unlink`. Проверка на наличие `LinkTo` (целевой сущности) присутствует.

**Статус:** ⚠️ Частично (фильтры отсутствуют полностью)
**TODO:** Нет

### Capabilities Coverage

**Filters:**
| SDK Filter Method | Bot Field | Status | Comment |
|-------------------|-----------|--------|---------|
| SetToEntityID | - | ❌ | Нельзя проверить связь с конкретным ID. |
| SetToEntityType | - | ❌ | Нельзя получить только связанные контакты. |
| SetToCatalogID | - | ❌ | |

**Batch Operations:**
- ❌ SDK: `Link`/`Unlink` принимают массив `[]*EntityLink`.
- ❌ Bot: Обрабатывает по одной связи через `LinkTarget`. Неэффективно для массового связывания (например, одного контакта с 10 сделками).

**With/Relations:**
- ℹ️ Links API обычно не поддерживает `with`.

**Рекомендации:**
1. Добавить `models.LinksFilter` в `ActivitiesInput`.
2. Поддержать массив `LinkTo []LinkTarget` для массовых операций.

---

## notes.go
**Layer:** notes
**Schema actions:** list, get, get_by_parent, create, update
**SDK service:** NotesService (`core/services/notes.go`)

| Метод SDK | Реализован в сервисе | Метод сервиса | Комментарий |
|-----------|----------------------|----------------|-------------|
| GetByParent| ✅ | `ListNotes` | Для списка по сущности |
| GetOne | ✅ | `GetNote` | |
| Create | ✅ | `CreateNote` | |
| Update | ✅ | `UpdateNote` | |

**Genkit Tool Handler:**
- 🛠 `handleNotes` полностью поддерживает `list`, `get`, `create` и `update`. Включает валидацию `ID` для `get`/`update` и `NoteData` для `create`/`update`.

**Статус:** ⚠️ Частично (фильтры отсутствуют полностью)
**TODO:** Нет

### Capabilities Coverage

**Filters:**
| SDK Filter Method | Bot Field | Status | Comment |
|-------------------|-----------|--------|---------|
| SetLimit | - | ❌ | Hardcoded 50. |
| SetPage | - | ❌ | Hardcoded 1. |
| SetIDs | - | ❌ | Нельзя получить конкретные примечания по ID. |
| SetEntityIDs | - | ❌ | |
| SetNoteTypes | - | ❌ | Нельзя отфильтровать только "входящие звонки". |
| SetUpdatedAt | - | ❌ | |

**Batch Operations:**
- ❌ SDK: `Create`/`Update` принимают `[]*Note`.
- ❌ Bot: Только по одному примечанию за раз.

**With/Relations:**
- ❌ Не используются.

**Рекомендации:**
1. Создать `models.NotesFilter` и добавить его в `ActivitiesInput`.
2. Позволить AI фильтровать примечания по типу (`note_type`).
3. Поддержать пагинацию.


---

## subscriptions.go
**Layer:** subscriptions
**Schema actions:** list, subscribe, unsubscribe
**SDK service:** EntitySubscriptionsService (`core/services/entity_subscriptions.go`)

| Метод SDK | Реализован в сервисе | Метод сервиса | Комментарий |
|-----------|----------------------|----------------|-------------|
| Get | ✅ | `ListSubscriptions`| Лимит 50 |
| Subscribe | ✅ | `Subscribe` | |
| Unsubscribe| ✅ | `Unsubscribe` | |

**Genkit Tool Handler:**
- 🛠 `handleSubscriptions` корректно обрабатывает `list`, `subscribe`, `unsubscribe`. Проверки на `UserIDs`/`UserID` на месте.

**Статус:** ✅ Полностью соответствует
**TODO:** Нет

### Capabilities Coverage

**Filters:**
- ℹ️ Подписки не имеют специфических фильтров в SDK (кроме пагинации).

**Batch Operations:**
- ✅ SDK: `Subscribe` принимает массив `user_ids`.
- ✅ Bot: `Subscribe` корректно передает массив.

**With/Relations:**
- ❌ Не используются.

**Рекомендации:**
1. Оставить как есть.


---

## tags.go
**Layer:** tags
**Schema actions:** list, create, delete
**SDK service:** TagsService (`core/services/tags.go`)

| Метод SDK | Реализован в сервисе | Метод сервиса | Комментарий |
|-----------|----------------------|----------------|-------------|
| Get | ✅ | `ListTags` | |
| Create | ✅ | `CreateTag` | |
| Update | ❌ | - | SDK поддерживает только для leads, в сервисе не реализовано |
| Delete | ⚠️ | `DeleteTag` | **Внимание:** SDK возвращает ошибку, так как API v4 не поддерживает прямое удаление тегов. |

**Genkit Tool Handler:**
- 🛠 `handleTags` поддерживает `list`, `create`, `delete`. 
- ⚠️ Экшен `delete` вызовет ошибку из SDK (`ErrTagsDeleteNotSupported`).

**Статус:** ⚠️ Частично (фильтры отсутствуют полностью)
**TODO:** 
1. Разобраться, нужно ли удаление тегов (обычно это делается через PATCH сущности с обнулением списка тегов).
2. Добавить `UpdateTag` для сделок, если это необходимо.

### Capabilities Coverage

**Filters:**
| SDK Filter Method | Bot Field | Status | Comment |
|-------------------|-----------|--------|---------|
| SetLimit | - | ❌ | Hardcoded 50. |
| SetPage | - | ❌ | Hardcoded 1. |
| SetIDs | - | ❌ | |
| SetQuery | - | ❌ | Нельзя искать теги по подстроке. |
| SetName | - | ❌ | Нельзя проверить существование конкретного тега по имени. |

**Batch Operations:**
- ❌ SDK: `Create`/`Update`/`Delete` принимают `[]*Tag`.
- ❌ Bot: По одному за раз.

**With/Relations:**
- ❌ Не используются.

**Рекомендации:**
1. Добавить `models.TagsFilter` в `ActivitiesInput`.
2. Реализовать `UpdateTag` для сделок.
3. Исправить логику удаления (предлагать патч сущности вместо вызова неработающего `Delete`).

---

## talks.go
**Layer:** talks
**Schema actions:** close (Get не поддерживается API)
**SDK service:** TalksService (`core/services/talks.go`)

| Метод SDK | Реализован в сервисе | Метод сервиса | Комментарий |
|-----------|----------------------|----------------|-------------|
| Get | ⚠️ | `ListTalks` | **Ошибка:** Реализован в боте, но SDK блокирует его (`ErrNotAvailableForAction`), так как API не поддерживает стандартный LIST для бесед. |
| Close | ✅ | `CloseTalk` | |
| GetOne | ✅ | - | В SDK есть, в боте не выведен |

**Genkit Tool Handler:**
- 🛠 `handleTalks` имеет кейсы `list` и `close`. 
- ⚠️ Кейс `list` вернёт ошибку, так как SDK (в соответствии с ограничениями API) не позволяет делать Get/List на беседы.

**Статус:** ⚠️ Требует исправления (удалить или поправить `list`)
**TODO:** 
1. Удалить экшен `list` из `handleTalks` и метод `ListTalks` из сервиса, так как он не поддерживается API/SDK.
2. В `tools_schema.md` уже указано, что Get не поддерживается — реализация в коде должна соответствовать.

### Capabilities Coverage

**Filters:**
- ❌ SDK: `Get` (list) заблокирован. Фильтрация невозможна.

**Batch Operations:**
- ❌ Не поддерживаются для этого типа сущности.

**Close Options:**
- ❌ SDK: `Close` принимает `TalkCloseOptions` (поле `force_close`).
- ❌ Bot: Не поддерживает принудительное закрытие.

**Рекомендации:**
1. Удалить нерабочий метод `ListTalks`.
2. Добавить `ForceClose` в параметры закрытия, если это необходимо.

---

## tasks.go
**Layer:** tasks
**Schema actions:** list, get, create, update, complete
**SDK service:** TasksService (`core/services/tasks.go`)

| Метод SDK | Реализован в сервисе | Метод сервиса | Комментарий |
|-----------|----------------------|----------------|-------------|
| Get | ✅ | `ListTasks` | Включает продвинутую фильтрацию (today, tomorrow, overdue, this_week, next_week) |
| GetOne | ✅ | `GetTask` | |
| Create | ✅ | `CreateTask` | Поддерживает человеко-читаемые даты через `utils.ParseHumanDeadline` |
| Update | ✅ | `UpdateTask` | Поддерживает человеко-читаемые даты |
| Complete | ✅ | `CompleteTask` | |

**Genkit Tool Handler:**
- 🛠 `handleTasks` поддерживает все действия: `list`, `get`, `create`, `update`, `complete`. Валидация входных данных присутствует.

**Статус:** ⚠️ Частично (фильтры и батчи не до конца реализованы)
**TODO:** Нет

### Capabilities Coverage

**Filters:**
| SDK Filter Method | Bot Field | Status | Comment |
|-------------------|-----------|--------|---------|
| SetLimit | filter.limit | ✅ | |
| SetPage | - | ❌ | Hardcoded 1 в сервисе. AI не может пагинировать. |
| SetOrder | - | ❌ | Hardcoded `complete_till asc`. AI не может менять сортировку. |
| SetResponsibleUserIDs | filter.responsible_user_id | ✅ | |
| SetIsCompleted | filter.is_completed | ✅ | |
| SetTaskTypeID | filter.task_type_id | ✅ | |
| SetEntityType | input.parent.type | ✅ | |
| SetEntityIDs | []int{input.parent.id} | ✅ | |
| SetIDs | - | ❌ | Нельзя запросить задачи по конкретному списку ID. |
| SetCreatedBy | - | ❌ | |
| SetUpdatedAt | - | ❌ | Нельзя найти "задачи, измененные сегодня". |
| SetLeadStatuses | - | ❌ | |

**Query Support:**
- ⚠️ Поле `filter.query` есть в модели бота, но **не поддерживается** в SDK и API amoCRM для задач. Оно игнорируется в коде.

**Batch Operations:**
- ❌ SDK: `Create`/`Update` принимают массив `[]*Task`.
- ❌ Bot: Обрабатывает строго по одному элементу (`TaskData`). Массовое создание задач (например, "создай 5 задач") будет неэффективным (сотни запросов).

**With/Relations:**
- ❌ Бот не использует `with` параметры. SDK поддерживает подгрузку связанных сущностей (`contact`, `lead`) прямо в ответе Get/List.

**Рекомендации:**
1. Вынести `IDs`, `UpdatedAt`, `Page` и `Order` в `models.TasksFilter`.
2. Добавить поддержку массового создания (например, `TasksData []TaskData` в `ActivitiesInput`).
3. Использовать `With` параметры, если LLM попросит "получить задачу вместе с контактом".

---
