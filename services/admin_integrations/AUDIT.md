# Аудит сервисов Admin Integrations

Этот файл содержит результаты последовательного аудита каждого сервиса в папке `services/admin_integrations/` на соответствие `tools_schema.md` и возможностям SDK.

---

## webhooks.go
**Layer:** webhooks
**Schema actions:** list, subscribe, unsubscribe
**SDK service:** WebhooksService (`core/services/webhooks.go`)

| Метод SDK | Реализован в сервисе | Метод сервиса | Комментарий |
|-----------|----------------------|----------------|-------------|
| Get | ✅ | `ListWebhooks` | Без фильтрации |
| Subscribe | ✅ | `SubscribeWebhook` | |
| Unsubscribe| ✅ | `UnsubscribeWebhook`| |

**Genkit Tool Handler:**
- 🛠 `handleWebhooks` поддерживает `list`, `subscribe`, `unsubscribe`. 
- 🛠 Корректно парсит `settings` из массива интерфейсов.

**Статус:** ⚠️ Частично (фильтры отсутствуют)
**TODO:** Нет

### Capabilities Coverage

**Filters:**
| SDK Filter Method | Bot Field | Status | Comment |
|-------------------|-----------|--------|---------|
| SetDestination | - | ❌ | Нельзя найти вебхук по конкретному URL. |

**Parameters:**
- ❌ SDK: `models.Webhook` имеет поле `Sort`.
- ❌ Bot: Не передается при подписке.

**Batch Operations:**
- ℹ️ API вебхуков работает строго по одному URL.

**With/Relations:**
- ℹ️ Не поддерживается для вебхуков.

**Рекомендации:**
1. Добавить фильтрацию по `destination` в `ListWebhooks`.
2. Добавить поддержку поля `sort` в `SubscribeWebhook`, если это потребуется.

## widgets.go
**Layer:** widgets
**Schema actions:** search, get, install, uninstall (create/update в боте не поддерживаются API)
**SDK service:** WidgetsService (`core/services/widgets.go`)

| Метод SDK | Реализован в сервисе | Метод сервиса | Комментарий |
|-----------|----------------------|----------------|-------------|
| Get | ✅ | `ListWidgets` | Без пагинации |
| GetByCode | ✅ | `GetWidget` | |
| InstallByCode | ✅ | `InstallWidget` | |
| Uninstall | ✅ | `UninstallWidget`| |
| Add | ❌ | `CreateWidgets` | **Ошибка:** Реализован в боте, но SDK блокирует его (`ErrNotAvailableForAction`), так как API не поддерживает создание виджетов. |
| Update | ❌ | `UpdateWidgets` | **Ошибка:** Реализован в боте, но SDK блокирует его (`ErrNotAvailableForAction`). |

**Genkit Tool Handler:**
- 🛠 `handleWidgets` поддерживает все экшены, включая ошибочные `create`/`update`.
- ⚠️ Экшен `install` вызывает `InstallByCode`, что не позволяет передать `Settings` (настройки виджета).

**Статус:** ⚠️ Требует исправления (удалить create/update)
**TODO:** 
1. Удалить нерабочие методы `CreateWidgets` и `UpdateWidgets`.
2. В `handleWidgets` поддержать передачу `settings` для экшена `install` (использовать `Install` вместо `InstallByCode`).

### Capabilities Coverage

**Filters:**
| SDK Filter Method | Bot Field | Status | Comment |
|-------------------|-----------|--------|---------|
| SetLimit | - | ❌ | |
| SetPage | - | ❌ | |

**Parameters:**
- ❌ SDK: `Install` принимает объект `models.Widget` с мапой `Settings`.
- ❌ Bot: `InstallWidget` принимает только `code`. AI не может настроить виджет при установке.

**Batch Operations:**
- ℹ️ Не поддерживаются для этого типа сущности.

**With/Relations:**
- ❌ Не используются.

**Рекомендации:**
1. Удалить нерабочие методы.
2. Поддержать настройки при установке виджета.

---

## website_buttons.go
**Layer:** website_buttons
**Schema actions:** search, get, create, update
**SDK service:** WebsiteButtonsService (`core/services/website_buttons.go`)

| Метод SDK | Реализован в сервисе | Метод сервиса | Комментарий |
|-----------|----------------------|----------------|-------------|
| Get | ✅ | `ListWebsiteButtons` | Без пагинации и `with` |
| GetOne | ✅ | `GetWebsiteButton` | Без `with` |
| CreateAsync | ✅ | `CreateWebsiteButton` | |
| UpdateAsync | ✅ | `UpdateWebsiteButton` | |
| AddOnlineChat | ❌ | - | Не реализовано в боте. |

**Genkit Tool Handler:**
- 🛠 `handleWebsiteButtons` поддерживает `list`, `get`, `create`, `update`.
- ⚠️ Не поддерживает экшен для `AddOnlineChat`.

**Статус:** ⚠️ Частично (отсутствуют `with` и доп. методы)
**TODO:** 
1. Реализовать поддержку `with=scripts` для получения кода кнопки.
2. Добавить экшен `add_chat` для привязки онлайн-чата к кнопке.

### Capabilities Coverage

**Filters:**
| SDK Filter Method | Bot Field | Status | Comment |
|-------------------|-----------|--------|---------|
| Page | - | ❌ | |
| Limit | - | ❌ | |

**Parameters:**
- ❌ SDK: `Get` и `GetOne` поддерживают `with[scripts, deleted]`.
- ❌ Bot: Не запрашивает `scripts`. AI не сможет выдать пользователю готовый код для вставки на сайт.

**Batch Operations:**
- ℹ️ Не поддерживаются для этого типа сущности.

**With/Relations:**
- ⚠️ Поддержка `with` есть в SDK, но не используется в боте.

**Рекомендации:**
1. Обязательно добавить `with=scripts` в `GetWebsiteButton`.
2. Реализовать `AddOnlineChat`.

## chat_templates.go
**Layer:** chat_templates
**Schema actions:** search, list, delete, send_review, update_review
**SDK service:** ChatTemplatesService (`core/services/chat_templates.go`)

| Метод SDK | Реализован в сервисе | Метод сервиса | Комментарий |
|-----------|----------------------|----------------|-------------|
| Get | ✅ | `ListChatTemplates` | Без пагинации и фильтров |
| Delete | ✅ | `DeleteChatTemplate` | Один за раз |
| DeleteMany | ❌ | - | Не реализовано (батч-удаление) |
| SendOnReview | ✅ | `SendChatTemplateOnReview` | |
| UpdateReviewStatus | ✅ | `UpdateChatTemplateReviewStatus` | |

**Genkit Tool Handler:**
- 🛠 `handleChatTemplates` поддерживает `list`, `delete`, `send_review`, `update_review`.
- ⚠️ `delete` только для одного ID.

**Статус:** ⚠️ Частично (отсутствуют фильтры и батчи)
**TODO:** 
1. Добавить поддержку `TemplatesFilter` (по `external_id`).
2. Реализовать батч-удаление через `DeleteMany`.

### Capabilities Coverage

**Filters:**
| SDK Filter Method | Bot Field | Status | Comment |
|-------------------|-----------|--------|---------|
| SetExternalIDs | - | ❌ | AI не может фильровать по внешним ID. |
| Page | - | ❌ | |
| Limit | - | ❌ | |

**Parameters:**
- ℹ️ Обновление статуса ревью принимает `status` строкой (OK).

**Batch Operations:**
- ❌ SDK: `DeleteMany` позволяет удалять массив шаблонов.
- ❌ Bot: AI удаляет по одному.

**With/Relations:**
- ℹ️ Не поддерживается.

**Рекомендации:**
1. Добавить `filter.external_ids` в инструмент.
2. Внедрить `DeleteMany`.

---

## short_links.go
**Layer:** short_links
**Schema actions:** search, list, create, delete
**SDK service:** ShortLinksService (`core/services/short_links.go`)

| Метод SDK | Реализован в сервисе | Метод сервиса | Комментарий |
|-----------|----------------------|----------------|-------------|
| Get | ✅ | `ListShortLinks` | Без пагинации |
| Create | ✅ | `CreateShortLink` | Использует батч-метод для одной ссылки |
| CreateOne | ❌ | - | Есть в SDK, не используется в боте |
| Delete | ✅ | `DeleteShortLink` | |

**Genkit Tool Handler:**
- 🛠 `handleShortLinks` поддерживает `list`, `create`, `delete`.
- ⚠️ `create` только по одной ссылке.

**Статус:** ✅ Полностью соответствует (учитывая сценарии AI)
**TODO:** Нет

### Capabilities Coverage

**Filters:**
| SDK Filter Method | Bot Field | Status | Comment |
|-------------------|-----------|--------|---------|
| Page | - | ❌ | |
| Limit | - | ❌ | |

**Parameters:**
- ℹ️ `Create` принимает массив объектов с полем `url`.

**Batch Operations:**
- ⚠️ SDK: `Create` позволяет создавать массив коротких ссылок.
- ⚠️ Bot: Создаёт по одной (редкий кейс для AI создавать сразу много ссылок).

**With/Relations:**
- ℹ️ Не поддерживается.

**Рекомендации:**
1. Можно оставить как есть, так как массовое создание ссылок — редкий сценарий.

---
